export default function OfflinePage() {
  return (
    <section className="mx-auto max-w-3xl px-4 py-24 text-center sm:px-6 lg:px-8">
      <p className="text-sm font-semibold uppercase tracking-[0.2em] text-ember">Offline</p>
      <h1 className="mt-4 font-serif text-5xl font-semibold text-ink">Saved pages are still available.</h1>
      <p className="mt-5 text-muted">Reconnect to refresh your reading history, comments, memberships, and newsletters.</p>
    </section>
  );
}
